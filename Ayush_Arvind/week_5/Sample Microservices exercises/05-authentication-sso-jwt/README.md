# Authentication, SSO and JWT Exercises

The examples are formatted for Spring Boot 3 and use
`SecurityFilterChain`, replacing the removed
`WebSecurityConfigurerAdapter`.

Replace placeholder client credentials and issuer URLs before running.
