# Spring Hands-On 1 - Spring Core XML and Logging

Covers project creation, `@SpringBootApplication`, XML bean loading, `SimpleDateFormat`, `Country` bean, and SLF4J logging.

## Run
```bash
cd spring-learn
mvn clean package
mvn spring-boot:run
```
