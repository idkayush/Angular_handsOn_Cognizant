# Spring Hands-On 3 - Employee and Department REST

Implements REST services `/employees`, `/employees/{id}`, and `/departments` using Controller-Service-DAO architecture.

## Run
```bash
cd spring-learn
mvn spring-boot:run
```
